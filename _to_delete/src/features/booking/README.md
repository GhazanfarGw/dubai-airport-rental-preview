# Booking feature (not built yet)

This folder is a placeholder. The customer-facing booking journey
(search → vehicle select → customer/driver details → payment →
confirmation) is scoped for a later phase, against the domain foundation
already in place in `supabase/migrations/`.

See `docs/ARCHITECTURE.md` for the full customer journey this will implement.
